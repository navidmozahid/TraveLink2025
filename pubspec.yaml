name: travelink
description: "A travel social media and booking application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔹 Supabase
  supabase_flutter: ^2.10.1

  # 🔹 Media & uploads
  image_picker: ^1.0.7
  uuid: ^4.3.3
  video_player: ^2.8.6

  # 🔹 REQUIRED for Instagram-style video autoplay
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
